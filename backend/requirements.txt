# --- Web framework ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# --- Database ---
sqlalchemy>=2.0.25

# --- Settings / config ---
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
# Required for FastAPI's File(...)/UploadFile (multipart form parsing).
python-multipart>=0.0.9

# --- Transcription ---
# Whisper transcribes every upload locally (no YouTube captions to fall back
# from). NOTE: pulls in torch and is heavy (multi-GB download).
openai-whisper>=20231117

# ffmpeg and ffprobe are required system binaries (not pip packages) - must
# be on PATH. Used for clip cutting/captioning and duration probing.

# --- Dev / lint / test ---
ruff>=0.2.0
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.26.0
